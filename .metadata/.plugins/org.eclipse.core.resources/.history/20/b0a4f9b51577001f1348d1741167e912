package area;

public class InsertionSort {

	public static Area[] sortByStationname(Area[] area) {
		for (int i = 1; i < area.length; i++) {
			int j = i;
			while (j > 0 && area[j - 1].getStationname().compareTo(area[i].getStationname()) > 0) {
				area[j] = area[j - 1];
				j--;
			}
			area[j] = area[i];
		}
		return area;
	}
	
	
	public static Area[] sortByStno(Area[] area) {
		for (int i = 1; i < area.length; i++) {
			int j = i;
			while (j > 0 && area[j - 1].getStno().compareTo(area[i].getStno()) > 0) {
				area[j] = area[j - 1];
				j--;
			}
			area[j] = area[i];
		}
		return area;
	}
	
	public static Area[] sortByTemperature(Area[] area) {
		for (int i = 1; i < area.length; i++) {
			int j = i;
			while (j > 0 && area[j - 1].getTemperature()>area[i].getTemperature()) {
				area[j] = area[j - 1];
				j--;
			}
			area[j] = area[i];
		}
		return area;
	}
	
	public static Area[] sortByColor(Area[] area) {
		for (int i = 1; i < area.length; i++) {
			int j = i;
			while (j > 0 && area[j - 1].getColor().compareTo(area[i].getColor()) > 0) {
				area[j] = area[j - 1];
				j--;
			}
			area[j] = area[i];
		}
		return area;
	}
	
	
	public static Area[] sortByPh(Area[] area) {
		for (int i = 1; i < area.length; i++) {
			int j = i;
			while (j > 0 && area[j - 1].getPh()>area[i].getPh()) {
				area[j] = area[j - 1];
				j--;
			}
			area[j] = area[i];
		}
		return area;
	}
	public static Area[] sortByChloride(Area[] area) {
		for (int i = 1; i < area.length; i++) {
			int j = i;
			while (j > 0 && area[j - 1].getChloride()>area[i].getChloride()) {
				area[j] = area[j - 1];
				j--;
			}
			area[j] = area[i];
		}
		return area;
	}
	public static Area[] sortByFlouride(Area[] area) {
		for (int i = 1; i < area.length; i++) {
			int j = i;
			while (j > 0 && area[j - 1].getFlouride()>area[i].getFlouride()) {
				area[j] = area[j - 1];
				j--;
			}
			area[j] = area[i];
		}
		return area;
	}

}
